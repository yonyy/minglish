Given a sorted dictionary (array of words) of an alien language, find order of characters in the language.

Creates a DAG and then performs topological sort.

Implements a hashmap/dict to get access to a specific vertex in the map and create an edge. 